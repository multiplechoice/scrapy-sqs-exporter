language: python

cache: pip

python:
  - '2.7'

install:
  - pip install -r requirements.txt
  - pip install coveralls

script:
  - pytest --doctest-modules --doctest-report ndiff sqsfeedexport

after_success:
  - coveralls
